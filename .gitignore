node_modules/
dist/
*.local
.env
.env.*
!.env.example
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
.DS_Store
coverage/

# Generated PDF intermediates (the PDFs themselves are kept, so a reviewer
# browsing the repo can read the notes without building them).
submission/*.html
api/_app.js
